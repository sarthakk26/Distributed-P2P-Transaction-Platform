# Test database — points to the Docker test container (port 5433)
DATABASE_URL="postgresql://testuser:testpassword@localhost:5433/wallet_test"

# Webhook signature secret — must match what the webhook service reads
BANK_WEBHOOK_SECRET="test-secret-do-not-use-in-prod"

# Node env
NODE_ENV="test"
