export default function TransitionLogs() {
  return (
    <div>
      <h2 className="font-semibold mb-2">Recent Transition Logs</h2>
      <p className="text-slate-500 text-sm">
        Transition logs visible via server logs for now.
      </p>
    </div>
  );
}
